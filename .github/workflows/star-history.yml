name: Update star history

on:
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - .github/workflows/star-history.yml
      - scripts/update-star-history.mjs

permissions:
  contents: read

concurrency:
  group: star-history
  cancel-in-progress: false

jobs:
  update:
    if: github.repository == 'NVlabs/SoL-Pi' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
      - name: Check out the original Star History renderer
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          repository: star-history/star-history
          ref: c326eac651bc5afb4cd40d354223dd419e1e2ae6
          path: .star-history-renderer
          persist-credentials: false
          sparse-checkout: |
            shared
            backend
      - name: Install renderer dependencies from its lockfiles
        run: |
          npm install --global --ignore-scripts pnpm@9.15.9
          pnpm --dir .star-history-renderer install --frozen-lockfile --ignore-scripts
          pnpm --dir .star-history-renderer/backend install --frozen-lockfile --ignore-scripts
      - name: Generate both themes from GitHub data
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          STAR_HISTORY_RENDERER: ${{ github.workspace }}/.star-history-renderer
        run: .star-history-renderer/backend/node_modules/.bin/tsx scripts/update-star-history.mjs "$GITHUB_REPOSITORY" "$RUNNER_TEMP/star-history"
      - name: Publish generated files
        shell: bash
        run: |
          if git ls-remote --exit-code --heads origin star-history; then
            git fetch --depth=1 origin star-history
            git switch -C star-history FETCH_HEAD
          else
            status=$?
            if [ "$status" -ne 2 ]; then
              exit "$status"
            fi
            git switch --orphan star-history
          fi
          for file in star-history-light.svg star-history-dark.svg star-history.json LICENSE-star-history.txt; do
            cp "$RUNNER_TEMP/star-history/$file" "$file"
          done
          git add -- star-history-light.svg star-history-dark.svg star-history.json LICENSE-star-history.txt
          if git diff --cached --quiet; then
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'docs: update star history'
          git push origin HEAD:refs/heads/star-history
